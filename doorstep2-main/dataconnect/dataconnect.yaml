specVersion: "v1"
serviceId: "doorstep"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "doorstep-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
